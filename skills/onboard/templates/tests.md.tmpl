---
agent: {{ name }}
version: 1
---

# Tests for {{ name }}

User-authored ground truth. Every block is a deterministic test case — no LLM
judgment in the scoring path. See
[`skills/shared/execute/SKILL.md`](../../skills/shared/execute/SKILL.md)
for all supported match types and field reference.

<!-- Scoring mode: all tests below use binary scoring (default).
     Add `partial_credit: true` to a test block to enable gradient scoring
     (score_t = sample_rate instead of 0/1) for near-miss tolerance. -->

~~~test
id: t1
name: <!-- TODO: describe what this test checks -->
input: "<!-- TODO: test input -->"
match: contains
expected: "<!-- TODO: expected substring in output -->"
samples: 3
pass_rate: 1.0
~~~

~~~test
id: t2
name: <!-- TODO: describe what this test checks -->
input: "<!-- TODO: test input -->"
match: contains
expected: "<!-- TODO: expected substring in output -->"
samples: 3
pass_rate: 1.0
~~~

~~~test
id: t3
name: <!-- TODO: describe edge case (e.g. empty input, special characters) -->
input: "<!-- TODO: edge case input -->"
match: contains
expected: "<!-- TODO: expected output for edge case -->"
samples: 3
pass_rate: 1.0
~~~

<!-- Add more ~~~test~~~ blocks as needed. Run:
       improver bootstrap <path>
     to validate this file and generate rubric.md. -->
