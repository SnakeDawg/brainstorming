---
{{ kind_field }}: {{ name }}
version: 1
baseline_score: null
epsilon: 0.05
---

# Rubric for {{ name }}

The scoring contract. Every `~~~test~~~` block below is an evaluation rule
with an embedded `weight`. The scorer reads this file as the sole source
of truth for accept/reject decisions. See
[`skills/shared/execute/SKILL.md`](../../skills/shared/execute/SKILL.md)
for all supported match types and the full field reference.

<!-- Scoring mode: all rules below use binary scoring (default).
     Add `partial_credit: true` to a rule block to enable gradient scoring
     (score_t = sample_rate instead of 0/1) for near-miss tolerance.
     Weights MUST sum to 1.0 (tolerance 0.01). -->

## Evaluation rules

~~~test
id: t1
name: <!-- TODO: describe what this rule checks -->
weight: 0.34
input: "<!-- TODO: rule input -->"
match: contains
expected: "<!-- TODO: expected substring in output -->"
samples: 3
pass_rate: 1.0
~~~

~~~test
id: t2
name: <!-- TODO: describe what this rule checks -->
weight: 0.33
input: "<!-- TODO: rule input -->"
match: contains
expected: "<!-- TODO: expected substring in output -->"
samples: 3
pass_rate: 1.0
~~~

~~~test
id: t3
name: <!-- TODO: describe edge case (e.g. empty input, special characters) -->
weight: 0.33
input: "<!-- TODO: edge case input -->"
match: contains
expected: "<!-- TODO: expected output for edge case -->"
samples: 3
pass_rate: 1.0
~~~

## Acceptance criterion

Accept iff:
  1. candidate_score ≥ baseline_score + epsilon
  2. zero regressions
  3. no blocking advisory critic raised

## Improvement policy

```yaml
improvement_policy:
  trigger: manual
  max_iterations: 3
  saturated_iterations: 6
  saturation_threshold: 0.95
```

## Advisory critics (NOT scored)

<!-- Add optional qualitative critics here. They run against candidate outputs
     but NEVER affect the numeric verdict unless marked `blocking: true`. -->

<!-- Add more ~~~test~~~ blocks as needed, then run:
       improver bootstrap <path>
     to measure the first baseline_score. -->
