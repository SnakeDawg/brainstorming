{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "permissions": {
    "allow": [
      "Write(.working/**)",
      "Write(outputs/**)",
      "Bash(mkdir -p .working*)",
      "Bash(mkdir -p outputs*)"
    ]
  }
}
