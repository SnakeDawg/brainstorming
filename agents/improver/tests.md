---
agent: improver
version: 1
mode: deferred
---

# Tests for improver

> **Status: deferred.** Improver is infrastructure. Its behavior is most
> naturally tested by the outcomes of running it against other agents —
> i.e. the `memory/runs/` trail *is* improver's test suite in practice.
> A dedicated self-test suite is out of scope for MVP-0 and is gated
> behind `self_improvable: true` in [`AGENT.md`](./AGENT.md).

This file exists so improver satisfies the scaffold's universal rule:
**every agent ships `tests.md`**. When self-improvement of improver is
unlocked in a later MVP, the human-authored deterministic tests will go
here in the standard `~~~test` fenced-block format.

## Placeholder

~~~test
id: t1
mode: deferred
reason: "Self-test suite not yet authored. See AGENT.md."
~~~
